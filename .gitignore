# Test-related
tests/debug/
.test-cache/

# Dependencies
node_modules/
package-lock.json
yarn.lock

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.local

# Vercel
.vercel

# Security - Service Account Credentials (NIEMALS einchecken!)
service-account-key.json
service-account-*.json
.calendar-ids.json
credentials.json
token.json
*-service-account-*.json

# Unencrypted configuration (NIEMALS einchecken!)
# This file contains sensitive user data and should only exist locally
config/authorized-users.json
config/authorized-users.unencrypted.json

# Setup Configuration with PII (NIEMALS einchecken!)
# Template: .setup-config.example.json (in repo für Referenz)
# Actual config: .setup-config.json (lokal, mit E-Mails)
.setup-config.json

# Encryption utility output
encrypt-config.log

# Generated IDs und temporäre Dateien
.calendar-id.json
.calendar-ids.json
credentials.json
token.json
